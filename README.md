# VectorGate
VectorGate is a RAG-based backend system where I used FAISS and HuggingFace embeddings to store and retrieve knowledge efficiently. It processes user queries through FastAPI and returns relevant results using semantic search.
